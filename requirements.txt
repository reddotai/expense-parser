# Core dependencies
pyyaml>=6.0
pandas>=2.0.0
pillow>=10.0.0
openpyxl>=3.1.0
xlsxwriter>=3.1.0
python-dotenv>=1.0.0  # For .env file support

# OCR (local processing, no API needed)
pytesseract>=0.3.10

# AI providers (install what you use)
openai>=1.0.0
anthropic>=0.8.0

# Optional: PDF support
# pymupdf>=1.23.0  # For PDF receipt processing
